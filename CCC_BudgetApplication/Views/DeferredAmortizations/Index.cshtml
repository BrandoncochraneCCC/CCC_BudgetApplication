@model IEnumerable<Application.Models.DeferredAmortization>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PoolBalance)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AccumulatedAmortization)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Year)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pool.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PoolBalance)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AccumulatedAmortization)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Year)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pool.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DeferredAmortizationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DeferredAmortizationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DeferredAmortizationID })
        </td>
    </tr>
}

</table>
